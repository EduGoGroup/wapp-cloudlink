# Binarios compilados (anclados a la raíz para no ignorar cmd/cloudlink/)
/cloudlink
/wapp-cloudlink
*.exe
*.exe~
*.test
*.out

# Directorios de salida
bin/
dist/

# Entornos y secretos
.env
.env.*

# Protobuf generado: SE COMMITEA (vive en gen/, importable cross-repo por el Edge).
# No se ignora a propósito.

# go workspace — SOLO desarrollo local (overlay de wapp-shared sin publicar)
go.work
go.work.sum

# Vendor (descomentar si se usa go mod vendor)
# vendor/

# macOS
.DS_Store

# IDEs
.idea/
.vscode/
*.swp
*.swo
